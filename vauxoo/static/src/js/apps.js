odoo.define('theme_vauxoo.apps', function (require) {
    "use strict";
    var base = require('web_editor.base');
    var core = require('web.core');
    if(!$('.app-check').length) {
        return $.Deferred().reject("DOM doesn't contain a app buton check");
    }
});
