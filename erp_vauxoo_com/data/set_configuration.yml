-
  Hide the "Portal/Leads & Opportunities" menu and sub menus.
-
  !python {model: ir.ui.menu}: |
    self.write(cr, uid, [
        ref('crm_partner_assign.portal_leads'),
    ],
    {'groups_id': [(6, 0, [ref('base.group_no_one')])]}, context=context)

-
  Setting sale options.
  The order of this parameters is exactly the order shown in the view, what will not
  be selected parameter 0 what will be selected, parameter 1
-
  !record {model: sale.config.settings, id: default_sale_properties}:
    {'module_web_linkedin': 0,
     'module_mass_mailing': 0,
     'group_invoice_so_lines': 1,
     'group_invoice_deli_orders': 1,
     'task_work': 1,
     # This to options than follow are related with the value of tast_work
     # As they need to be triggered with a onchange I simply change them manually.
     # 'module_project_timesheet': 1, # It is dependency already
     # 'module_sale_service': 1, # It is dependency already
     'timesheet': 1,
     'default_order_policy': 'manual',
     # 'module_delivery': 1,  # It is dependency already
     'group_sale_pricelist': 1,
     # 'module_warning': 1,  # It is dependency already
     'group_sale_delivery_address': 1,
     # 'module_sale_stock': 1,  # It is dependency already
     'group_mrp_properties': 1,
     'module_sale_journal': 0,
     'default_picking_policy': 0,
     'group_uom': 1,
     'group_discount_per_so_line': 1,
     'module_website_quote': 0,
     'module_sale_margin': 0,
     'group_route_so_lines': 0,
     'module_account_analytic_analysis': 1,  # It is dependency already
     'module_analytic_user_function': 0,
     'group_template_required': 0,
     'group_scheduled_calls': 0,
     'module_crm_claim': 0,
     'module_crm_helpdesk': 0,
     'group_multi_salesteams': 1,
     'alias_prefix': 'info'
     }
-
  !python {model: sale.config.settings}: |
    self.execute(cr, uid, [ref('default_sale_properties')], context=context)
-
  Setting project options.
  The order of this parameters is exactly the order shown in the view, what will not
  be selected parameter 0 what will be selected, parameter 1
-
  !record {model: project.config.settings, id: default_project_properties}:
    {
     # 'module_project_timesheet': 1,  # Already configured in sales
     # 'module_sale_service': 1,  # Already configured in sales
     'module_pad': 1,
     'group_manage_delegation_task': 1,
     # 'module_project_issue': 1,  # It is dependency already
     # 'module_project_issue_sheet': 1,  # It is dependency already
     # Due we come from 7.0 it was configured manually.
     'fetchmail_issue': 0,
     }
-
  !python {model: project.config.settings}: |
    self.execute(cr, uid, [ref('default_project_properties')], context=context)

